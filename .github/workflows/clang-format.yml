name: clang-format
on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master
jobs:
  clang-format:
    runs-on: ubuntu-22.04
    steps:
    - uses: actions/checkout@v2
      with:
        path: "rasr"
    - name: Install clang-format
      run: |
        sudo apt-get update -qy
        sudo apt-get install -y clang-format-14
    - name: Setup Sisyphus environment
      run: |
        cd rasr
        find . -name "*.cc" -o -name "*.cu" -o -name "*.hh" | xargs clang-format-14 --dry-run --Werror
