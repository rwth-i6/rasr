set(EXE_NAMES "cart-trainer" "cart-viewer")
foreach(EXE_NAME ${EXE_NAMES})
    add_executable(${EXE_NAME} CartTrainer.cc)
    target_link_libraries(${EXE_NAME} PRIVATE RasrCart RasrCore)
    add_install_executable(${EXE_NAME})
endforeach ()